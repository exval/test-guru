  <% @questions.each do |question| %>
    <li> <%= question.body %></li>
  
  <td> <% link_to 'Edit', edit_question_path(question) %> </td>
  <td> <% link_to 'Delete',
                  tests_path,
                  method: :delete,
                  data: {confirm: 'Are you sure?'} %> 
  </td>
  <% end %>
